( define (f1 n ); зовнішня функція
  
    (define (f2 n i);  внутрішня функція
        
    ( if ( = i n); перевірка на глубину рукурсії
        ( +(mod  i  2) 1)
     (+ ( +(mod  i  2) 1)  ( / 1 (f2  n (+ i 1) ))  );
      
      )    
    
    )

    (f2 n 0 );запущення внутрішньої функції 

    )

(display( f1 5 )); виклик функції
(newline)